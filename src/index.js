import { getPbCrypto } from './crypto'; 
import { getEncryptedStorage, isBrowserSupported } from './encrypted-storage';

export default {
  getPbCrypto,
  getEncryptedStorage,
  isBrowserSupported,
};